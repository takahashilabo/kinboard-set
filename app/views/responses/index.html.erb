<% @responses.each do |res| %>
   <p>
       <%= @count = @count + 1 %>：
       <%= res.message %>
       <%= res.tdate %>
       <%= res.user_id %>
       <%= link_to '削除', response_path(res.id), method: :delete %>
    </p>
<% end %>

<p><%= link_to 'レスの追加', new_response_path %></p>
<p><%= link_to 'スレに戻る', thres_path %></p>
<p><%= link_to 'トップに戻る', top_main_path %></p>
